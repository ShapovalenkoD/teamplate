import React from "react";

import { TestComponent } from "@template/common";

export default {
  title: "components/TestComponent",
  component: TestComponent,
};

const Template = () => <TestComponent />;

export const Primary = Template.bind({});
